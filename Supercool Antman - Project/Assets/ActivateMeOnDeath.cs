using System.Collections;
using UnityEngine;

public class ActivateMeOnDeath : MonoBehaviour
{
/*    private void OnEnable()
    {
        PlayerStats.OnPlayerDeath += WrapperForActivatePanel;
    }

    private void OnDisable()
    {
        PlayerStats.OnPlayerDeath -= WrapperForActivatePanel;
    }

    private void Start()
    {
        
    }

    private IEnumerator ActivatePanel()
    {
        yield return new WaitForSeconds(4);
        bool isActivated = gameObject.activeSelf;
        print("suppose to activate panel");
    }

    private void WrapperForActivatePanel()
    {
        StartCoroutine(nameof(ActivatePanel));
    }*/
}
